"""Sunny — a personal AI assistant that lives on your PC.

Phase 1 capabilities:
- A Claude-powered brain (agentic tool-use loop).
- A two-way phone bridge over ntfy (push out, receive in).
- Persistent memory (SQLite) so she remembers across restarts.
- A mock device-control tool (Home Assistant slots in here later).
- A propose -> sandbox-test -> you-approve self-improvement loop.
"""

__version__ = "0.1.0"
